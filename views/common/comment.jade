if comment.CommentTypeId === 1
  a.embedly-card(href="#{comment.text}", data-card-width="100%" target="_blank") #{comment.text}
else
  blockquote.embedly-card
    strong !{comment.User.name}:&nbsp;
    | !{comment.text}